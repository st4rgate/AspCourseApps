@model StockTrade

@{
    ViewBag.Title = "Finnhub Stocks";
}

@*<form method="post">
    <div class="stock-box">
    <div class="title">
        <h2 class="title">@Model.StockName</h2>
    </div>

    <div class="details">
        <span class="label">Symbol: </span>
        <span class="value">@Model.StockSymbol</span>
    </div>

    <div class="details">
        <span class="label">Last Price: </span>
        <span class="price">@Model.Price</span>
        <span class="value">@Model.Currency</span>
    </div>
    </div>

    <input type="hidden" name="StockSymbol" id="StockSymbolID" value="@Model.StockSymbol" />
    <input type="hidden" name="Token" id="TokenID" value="@ViewBag.Token" />

</form>*@

<!-- Page Title -->
<div class="pagetitle">
    <h1>Stocks</h1>
    <nav>
        <ol class="breadcrumb">
            <li class="breadcrumb-item"><a href="index.html">Home</a></li>
            <li class="breadcrumb-item active">Stocks</li>
        </ol>
    </nav>
</div>
<!-- End Page Title -->

<section class="dashboard">
    <form method="post">
        <div class="row">
            <div class="col-lg-6">
                <div class="row">

                    <!-- Card -->
                    <div class="col-xxl-6 col-md-6">
                        <div class="card info-card revenue-card">
                            <div class="card-body">
                                <h5 class="card-title">@Model.StockName<span> | @Model.StockSymbol</span></h5>

                                <div class="d-flex align-items-center">
                                    <div class="card-icon rounded-circle d-flex align-items-center justify-content-center">
                                        <i class="bi bi-currency-dollar"></i>
                                    </div>
                                    <div class="ps-3">
                                        <h6>@Model.Currency @Model.Price</h6>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                    <!-- Card -->
                    <!-- Card -->
                    <div class="col-xxl-6 col-md-6">
                </div>
            </div>
        </div>

        <input type="hidden" name="StockSymbol" id="StockSymbolID" value="@Model.StockSymbol" />
        <input type="hidden" name="Token" id="TokenID" value="@ViewBag.Token" />
    </form>
</section>

@section JS{<script src="~/assets/js/FinnhubScript.js"></script>}
